diff --git a/namma_neighbor/apps/communities/migrations/0003_alter_community_invite_code.py b/namma_neighbor/apps/communities/migrations/0003_alter_community_invite_code.py
new file mode 100644
index 00000000..72ca9063
--- /dev/null
+++ b/namma_neighbor/apps/communities/migrations/0003_alter_community_invite_code.py
@@ -0,0 +1,18 @@
+# Generated by Django 5.1.15 on 2026-04-21 16:07
+
+from django.db import migrations, models
+
+
+class Migration(migrations.Migration):
+
+    dependencies = [
+        ('communities', '0002_community_full_schema'),
+    ]
+
+    operations = [
+        migrations.AlterField(
+            model_name='community',
+            name='invite_code',
+            field=models.CharField(blank=True, db_index=True, max_length=6, unique=True),
+        ),
+    ]
diff --git a/namma_neighbor/apps/vendors/migrations/0001_initial.py b/namma_neighbor/apps/vendors/migrations/0001_initial.py
new file mode 100644
index 00000000..016f4274
--- /dev/null
+++ b/namma_neighbor/apps/vendors/migrations/0001_initial.py
@@ -0,0 +1,72 @@
+# Generated by Django 5.1.15 on 2026-04-21 16:07
+
+import django.core.validators
+import django.db.models.deletion
+from decimal import Decimal
+from django.conf import settings
+from django.db import migrations, models
+
+
+class Migration(migrations.Migration):
+
+    initial = True
+
+    dependencies = [
+        ('communities', '0003_alter_community_invite_code'),
+        migrations.swappable_dependency(settings.AUTH_USER_MODEL),
+    ]
+
+    operations = [
+        migrations.CreateModel(
+            name='Vendor',
+            fields=[
+                ('id', models.BigAutoField(auto_created=True, primary_key=True, serialize=False, verbose_name='ID')),
+                ('created_at', models.DateTimeField(auto_now_add=True)),
+                ('updated_at', models.DateTimeField(auto_now=True)),
+                ('display_name', models.CharField(max_length=150)),
+                ('bio', models.TextField(blank=True)),
+                ('logistics_tier', models.CharField(choices=[('tier_a', 'Self-delivery, own bike/van'), ('tier_b', 'NammaNeighbor pickup required')], max_length=6)),
+                ('is_food_seller', models.BooleanField(default=False)),
+                ('govt_id_s3_key', models.CharField(blank=True, max_length=500)),
+                ('bank_proof_s3_key', models.CharField(blank=True, max_length=500)),
+                ('fssai_number', models.CharField(blank=True, max_length=14, validators=[django.core.validators.RegexValidator('^\\d{14}$', 'FSSAI number must be exactly 14 digits.')])),
+                ('fssai_status', models.CharField(choices=[('not_applicable', 'Not Applicable'), ('pending', 'Pending'), ('verified', 'Verified'), ('expired', 'Expired'), ('failed', 'Failed')], default='not_applicable', max_length=14)),
+                ('fssai_cert_s3_key', models.CharField(blank=True, max_length=500)),
+                ('fssai_verified_at', models.DateTimeField(blank=True, null=True)),
+                ('fssai_expiry_date', models.DateField(blank=True, db_index=True, null=True)),
+                ('fssai_business_name', models.CharField(blank=True, max_length=200)),
+                ('fssai_authorized_categories', models.JSONField(default=list)),
+                ('fssai_expiry_warning_sent', models.BooleanField(default=False)),
+                ('gstin', models.CharField(blank=True, max_length=15)),
+                ('gst_cert_s3_key', models.CharField(blank=True, max_length=500)),
+                ('razorpay_account_id', models.CharField(blank=True, max_length=100)),
+                ('razorpay_account_status', models.CharField(blank=True, max_length=20)),
+                ('razorpay_onboarding_step', models.CharField(blank=True, max_length=20)),
+                ('bank_account_verified', models.BooleanField(default=False)),
+                ('completed_delivery_count', models.PositiveIntegerField(default=0)),
+                ('average_rating', models.DecimalField(decimal_places=2, default=Decimal('0.00'), max_digits=3)),
+                ('user', models.OneToOneField(on_delete=django.db.models.deletion.CASCADE, related_name='vendor_profile', to=settings.AUTH_USER_MODEL)),
+            ],
+            options={
+                'abstract': False,
+            },
+        ),
+        migrations.CreateModel(
+            name='VendorCommunity',
+            fields=[
+                ('id', models.BigAutoField(auto_created=True, primary_key=True, serialize=False, verbose_name='ID')),
+                ('status', models.CharField(choices=[('pending_review', 'Pending Review'), ('approved', 'Approved'), ('rejected', 'Rejected'), ('suspended', 'Suspended')], max_length=14)),
+                ('approved_at', models.DateTimeField(blank=True, null=True)),
+                ('rejection_reason', models.TextField(blank=True)),
+                ('delist_threshold', models.PositiveIntegerField(default=2)),
+                ('missed_window_count', models.PositiveIntegerField(default=0)),
+                ('approved_by', models.ForeignKey(blank=True, null=True, on_delete=django.db.models.deletion.SET_NULL, related_name='vendor_approvals', to=settings.AUTH_USER_MODEL)),
+                ('community', models.ForeignKey(on_delete=django.db.models.deletion.PROTECT, related_name='vendor_memberships', to='communities.community')),
+                ('vendor', models.ForeignKey(on_delete=django.db.models.deletion.CASCADE, related_name='community_memberships', to='vendors.vendor')),
+            ],
+            options={
+                'indexes': [models.Index(fields=['community', 'status'], name='vendors_ven_communi_9b88e8_idx')],
+                'constraints': [models.UniqueConstraint(fields=('vendor', 'community'), name='unique_vendor_community')],
+            },
+        ),
+    ]
diff --git a/namma_neighbor/apps/vendors/migrations/__init__.py b/namma_neighbor/apps/vendors/migrations/__init__.py
new file mode 100644
index 00000000..e69de29b
diff --git a/namma_neighbor/apps/vendors/models.py b/namma_neighbor/apps/vendors/models.py
index 137941ff..897d019c 100644
--- a/namma_neighbor/apps/vendors/models.py
+++ b/namma_neighbor/apps/vendors/models.py
@@ -1 +1,110 @@
+from decimal import Decimal
+
+from django.conf import settings
+from django.core.validators import RegexValidator
 from django.db import models
+
+from apps.core.models import TimestampedModel
+
+
+class LogisticsTier(models.TextChoices):
+    TIER_A = "tier_a", "Self-delivery, own bike/van"
+    TIER_B = "tier_b", "NammaNeighbor pickup required"
+
+
+class FSSAIStatus(models.TextChoices):
+    NOT_APPLICABLE = "not_applicable", "Not Applicable"
+    PENDING = "pending", "Pending"
+    VERIFIED = "verified", "Verified"
+    EXPIRED = "expired", "Expired"
+    FAILED = "failed", "Failed"
+
+
+class VendorCommunityStatus(models.TextChoices):
+    PENDING_REVIEW = "pending_review", "Pending Review"
+    APPROVED = "approved", "Approved"
+    REJECTED = "rejected", "Rejected"
+    SUSPENDED = "suspended", "Suspended"
+
+
+class Vendor(TimestampedModel):
+    user = models.OneToOneField(
+        settings.AUTH_USER_MODEL,
+        on_delete=models.CASCADE,
+        related_name="vendor_profile",
+    )
+    display_name = models.CharField(max_length=150)
+    bio = models.TextField(blank=True)
+    logistics_tier = models.CharField(max_length=6, choices=LogisticsTier.choices)
+    is_food_seller = models.BooleanField(default=False)
+
+    govt_id_s3_key = models.CharField(max_length=500, blank=True)
+    bank_proof_s3_key = models.CharField(max_length=500, blank=True)
+
+    fssai_number = models.CharField(
+        max_length=14,
+        blank=True,
+        validators=[RegexValidator(r"^\d{14}$", "FSSAI number must be exactly 14 digits.")],
+    )
+    fssai_status = models.CharField(
+        max_length=14,
+        choices=FSSAIStatus.choices,
+        default=FSSAIStatus.NOT_APPLICABLE,
+    )
+    fssai_cert_s3_key = models.CharField(max_length=500, blank=True)
+    fssai_verified_at = models.DateTimeField(null=True, blank=True)
+    fssai_expiry_date = models.DateField(null=True, blank=True, db_index=True)
+    fssai_business_name = models.CharField(max_length=200, blank=True)
+    fssai_authorized_categories = models.JSONField(default=list, blank=True)
+    fssai_expiry_warning_sent = models.BooleanField(default=False)
+
+    gstin = models.CharField(max_length=15, blank=True)
+    gst_cert_s3_key = models.CharField(max_length=500, blank=True)
+
+    razorpay_account_id = models.CharField(max_length=100, blank=True)
+    razorpay_account_status = models.CharField(max_length=20, blank=True)
+    razorpay_onboarding_step = models.CharField(max_length=20, blank=True)
+    bank_account_verified = models.BooleanField(default=False)
+
+    completed_delivery_count = models.PositiveIntegerField(default=0)
+    average_rating = models.DecimalField(max_digits=3, decimal_places=2, default=Decimal("0.00"))
+
+    @property
+    def is_new_seller(self) -> bool:
+        """Return True until vendor has >= 5 deliveries AND >= 4.5 average rating."""
+        return self.completed_delivery_count < 5 or self.average_rating < Decimal("4.5")
+
+    def __str__(self) -> str:
+        return self.display_name
+
+
+class VendorCommunity(models.Model):
+    vendor = models.ForeignKey(Vendor, on_delete=models.CASCADE, related_name="community_memberships")
+    community = models.ForeignKey(
+        "communities.Community",
+        on_delete=models.PROTECT,
+        related_name="vendor_memberships",
+    )
+    status = models.CharField(max_length=14, choices=VendorCommunityStatus.choices)
+    approved_by = models.ForeignKey(
+        settings.AUTH_USER_MODEL,
+        null=True,
+        blank=True,
+        on_delete=models.SET_NULL,
+        related_name="vendor_approvals",
+    )
+    approved_at = models.DateTimeField(null=True, blank=True)
+    rejection_reason = models.TextField(blank=True)
+    delist_threshold = models.PositiveIntegerField(default=2)
+    missed_window_count = models.PositiveIntegerField(default=0)
+
+    class Meta:
+        constraints = [
+            models.UniqueConstraint(fields=["vendor", "community"], name="unique_vendor_community"),
+        ]
+        indexes = [
+            models.Index(fields=["community", "status"]),
+        ]
+
+    def __str__(self) -> str:
+        return f"{self.vendor} @ {self.community}"
diff --git a/namma_neighbor/apps/vendors/services/__init__.py b/namma_neighbor/apps/vendors/services/__init__.py
new file mode 100644
index 00000000..e69de29b
diff --git a/namma_neighbor/apps/vendors/services/fssai.py b/namma_neighbor/apps/vendors/services/fssai.py
new file mode 100644
index 00000000..d0167e0a
--- /dev/null
+++ b/namma_neighbor/apps/vendors/services/fssai.py
@@ -0,0 +1 @@
+# TODO: section-03
diff --git a/namma_neighbor/apps/vendors/services/razorpay.py b/namma_neighbor/apps/vendors/services/razorpay.py
new file mode 100644
index 00000000..2f2c3187
--- /dev/null
+++ b/namma_neighbor/apps/vendors/services/razorpay.py
@@ -0,0 +1 @@
+# TODO: section-04
diff --git a/namma_neighbor/apps/vendors/tests/conftest.py b/namma_neighbor/apps/vendors/tests/conftest.py
new file mode 100644
index 00000000..a8afd083
--- /dev/null
+++ b/namma_neighbor/apps/vendors/tests/conftest.py
@@ -0,0 +1,20 @@
+import pytest
+from rest_framework.test import APIClient
+
+from apps.users.tests.factories import UserFactory
+from apps.vendors.tests.factories import VendorFactory
+
+
+@pytest.fixture
+def api_client():
+    return APIClient()
+
+
+@pytest.fixture
+def vendor_user(db):
+    return UserFactory()
+
+
+@pytest.fixture
+def vendor(db):
+    return VendorFactory()
diff --git a/namma_neighbor/apps/vendors/tests/factories.py b/namma_neighbor/apps/vendors/tests/factories.py
new file mode 100644
index 00000000..4bf4e83e
--- /dev/null
+++ b/namma_neighbor/apps/vendors/tests/factories.py
@@ -0,0 +1,28 @@
+import factory
+
+from apps.communities.tests.factories import CommunityFactory
+from apps.users.tests.factories import UserFactory
+from apps.vendors.models import FSSAIStatus, LogisticsTier, Vendor, VendorCommunity, VendorCommunityStatus
+
+
+class VendorFactory(factory.django.DjangoModelFactory):
+    class Meta:
+        model = Vendor
+
+    user = factory.SubFactory(UserFactory)
+    display_name = factory.Faker("company")
+    logistics_tier = LogisticsTier.TIER_B
+    is_food_seller = False
+    fssai_status = FSSAIStatus.NOT_APPLICABLE
+    razorpay_onboarding_step = ""
+
+
+class VendorCommunityFactory(factory.django.DjangoModelFactory):
+    class Meta:
+        model = VendorCommunity
+
+    vendor = factory.SubFactory(VendorFactory)
+    community = factory.SubFactory(CommunityFactory)
+    status = VendorCommunityStatus.PENDING_REVIEW
+    delist_threshold = 2
+    missed_window_count = 0
diff --git a/namma_neighbor/apps/vendors/tests/test_models.py b/namma_neighbor/apps/vendors/tests/test_models.py
new file mode 100644
index 00000000..7a0e7a8d
--- /dev/null
+++ b/namma_neighbor/apps/vendors/tests/test_models.py
@@ -0,0 +1,55 @@
+from decimal import Decimal
+
+import pytest
+from django.core.exceptions import ValidationError
+from django.db import IntegrityError
+
+from apps.vendors.models import VendorCommunityStatus
+from apps.vendors.tests.factories import VendorCommunityFactory, VendorFactory
+
+
+@pytest.mark.django_db
+class TestVendorModel:
+    def test_is_new_seller_true_when_low_delivery_count(self):
+        vendor = VendorFactory(completed_delivery_count=4, average_rating=Decimal("4.8"))
+        assert vendor.is_new_seller is True
+
+    def test_is_new_seller_true_when_low_rating(self):
+        vendor = VendorFactory(completed_delivery_count=10, average_rating=Decimal("4.4"))
+        assert vendor.is_new_seller is True
+
+    def test_is_new_seller_false_when_both_thresholds_met(self):
+        vendor = VendorFactory(completed_delivery_count=5, average_rating=Decimal("4.5"))
+        assert vendor.is_new_seller is False
+
+    def test_fssai_number_rejects_short_string(self):
+        vendor = VendorFactory.build(fssai_number="1234567")
+        with pytest.raises(ValidationError):
+            vendor.full_clean()
+
+    def test_fssai_number_rejects_non_digit_characters(self):
+        vendor = VendorFactory.build(fssai_number="1234567890123A")
+        with pytest.raises(ValidationError):
+            vendor.full_clean()
+
+    def test_fssai_number_accepts_valid_14_digit_string(self):
+        vendor = VendorFactory.build(fssai_number="12345678901234")
+        vendor.full_clean(exclude=["user"])  # should not raise
+
+    def test_vendor_user_is_one_to_one(self, db):
+        vendor = VendorFactory()
+        with pytest.raises(IntegrityError):
+            VendorFactory(user=vendor.user)
+
+
+@pytest.mark.django_db
+class TestVendorCommunityModel:
+    def test_unique_vendor_community_constraint(self):
+        vc = VendorCommunityFactory()
+        with pytest.raises(IntegrityError):
+            VendorCommunityFactory(vendor=vc.vendor, community=vc.community)
+
+    def test_filter_by_community_and_status(self):
+        vc = VendorCommunityFactory(status=VendorCommunityStatus.APPROVED)
+        result = vc.__class__.objects.filter(community=vc.community, status=VendorCommunityStatus.APPROVED)
+        assert vc in result
diff --git a/namma_neighbor/apps/vendors/tests/test_services.py b/namma_neighbor/apps/vendors/tests/test_services.py
new file mode 100644
index 00000000..27fe6e81
--- /dev/null
+++ b/namma_neighbor/apps/vendors/tests/test_services.py
@@ -0,0 +1 @@
+# TODO: sections 03, 04
diff --git a/namma_neighbor/apps/vendors/tests/test_tasks.py b/namma_neighbor/apps/vendors/tests/test_tasks.py
new file mode 100644
index 00000000..15ff4006
--- /dev/null
+++ b/namma_neighbor/apps/vendors/tests/test_tasks.py
@@ -0,0 +1 @@
+# TODO: section-09
diff --git a/namma_neighbor/apps/vendors/tests/test_views.py b/namma_neighbor/apps/vendors/tests/test_views.py
new file mode 100644
index 00000000..682f93a0
--- /dev/null
+++ b/namma_neighbor/apps/vendors/tests/test_views.py
@@ -0,0 +1 @@
+# TODO: sections 07, 08
